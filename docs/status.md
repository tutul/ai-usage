# 現況

下一步見 [TODO.md](TODO.md)。

**最後更新：2026-09-08**

> 🟢 憑證續期與 Keychain 授權都已解決（D-016）。app 以 Apple Development 憑證簽章，
> 分區清單改以 teamid 釘住，不再反覆要求鑰匙圈密碼。

## 目前可用

| 功能 | 狀態 |
|---|---|
| Claude weekly / session 追蹤 | ✅ 運作中 |
| Codex weekly / session 追蹤 | ✅ 運作中 |
| Claude 憑證自動續期 | ✅ **已於 2026-08-29 20:36 實際續期成功**，CLI 登入未受影響 |
| SQLite 永久記錄 + SQL view 推導 | ✅ |
| menu bar 圖示（用量變色、停擺示警） | ✅ |
| 歷史圖表（**小時／日／週**三粒度、折線、hover 明細、重新整理） | ✅ |
| 日期範圍選擇（起迄 + 快速選擇） | ✅ |
| 視窗內取樣日誌（一頁 50 筆、依日分組、可載入更多） | ✅ |
| 快取分頁（**Claude + Codex**，按專案 × 日的 token 分布） | ✅ 手動匯入 |
| 個別關閉 Claude／Codex 的追蹤 | ✅ 選單內可切換 |
| 開機自啟（`SMAppService`） | ✅ 選單內可切換 |

資料累積起點：2026-08-28 22:32:40，目前 7,582 筆樣本、9,966 筆快取請求紀錄。
測試：41 個，`cd Packages/AIUsageKit && swift test`。
Migration：001–009 已套用。
簽章：Apple Development（Personal Team），`TeamIdentifier=2PUS5K7TA4`。

## 已知限制（設計上接受的）

- **app 必須開著才有資料。** 已可開機自啟，但**建議把 app 搬到 `/Applications`** ——
  從建置目錄註冊會把該路徑寫進登入項目，目錄一清就失效（UI 會提醒）。
- **取樣間隔不保證，且這是設計上接受的。** `qualityOfService = .utility` 明示
  系統可在電源／散熱吃緊時延後。實測 08-28～29 平均 2.9 分、100% 在 6 分內；
  08-30 平均 6.7 分、82% 在 6 分內，非睡眠時段也出現過連續 25–35 分的間隔。
  **目前的頻率已足夠**：跨整點的樣本對全都落在門檻內，實際歸屬率接近 100%。
- **Codex 解析度 1%**（整數百分比），小時層級偏粗。
- **delta 加總是近似值**（`regress` 夾擠為 0 會微幅高估）。
  精確值讀 `v_window_summary.used_percent`。
- **本地時區分桶由讀取端 TZ 決定。**
- **端點皆未公開**，可能無預警變更。
- **「閒置後重寫」只對 Claude 有意義。** 門檻（1 小時）來自實測 Claude 的快取
  幾乎全是 1 小時 TTL；**Codex 的 TTL 未知且無從觀測**，所以那一欄對 Codex
  一律留白，不套用 Claude 的門檻。
- **兩家的快取分頁分開看，不可混算。** 機制不同 —— 實測涵蓋率 Claude 98.1%、
  Codex 48.4%，混在同一組摘要數字裡沒有意義。
- **快取匯入是手動的。** 按分頁右上角的「匯入」。可重複執行，不會產生重複。

## 觀察中

- ~~**Keychain 反覆要求密碼**~~ —— ✅ **已解決（2026-09-05，見 D-016）**。
  成因是續期寫回會重設該項目的分區清單。改為**讀 Claude Code 的項目、只寫本 app
  自己的 `AIUsage-claude-credentials`**。代價：`claude` CLI 需重新登入一次
  （使用者確認不使用 CLI）。續期鏈失效時會自動重新種子，不需手動處理。

- **背景取樣完成後圖表是否自動更新** —— 理論上 `@Observable` 會處理，
  但曾有使用者回報需手動重新整理。已加重新整理鈕與焦點自動重讀作為保險，
  但根因尚未確認（可能只是取樣間隔太長造成的錯覺）。
- ~~**OpenAI 的外部提前重置**~~ —— ✅ **兩天內觀測到兩次**，`ended_early` 皆正確標記：
  | # | 重置發生於 | 錨點後 | 當時用量 |
  |---|---|---|---|
  | 1 | 2026-08-30 05:16～06:31 | 16.1 小時 | 3% |
  | 2 | 2026-08-31 10:04～10:27 | 26.1 小時 | 1% |

  全程 `plan_type` 與回應結構未變，排除換方案／換帳號／端點改版。已再次驗證
  **不是移動窗**（見 D-008）。外部佐證：OpenAI 2026-08 曾多次對全體付費用戶
  提前清空額度。
- ~~**重置頻率會不會回歸正常**~~ —— ✅ **假說成立**。2026-09-01 00:03 開始的窗
  完整跑滿七天、於 09-07 23:56 準時重置、`ended_early=0`、峰值 19%。
  08-30／08-31 那兩次提前重置確實是 08-25 恢復 5 小時上限後的過渡期現象。
  「一個窗 ≈ 一週」的前提恢復，週粒度已照原設計實作。
- **Codex 5 小時窗回歸後的行為**（2026-08-25 對 Plus 恢復）。
